# Cadence ops engine database configuration template.
# Copy to engine.properties next to this file (or pass an explicit path
# with --db-config=/path/to/engine.properties). engine.properties is
# gitignored and must never be committed.

db.host=127.0.0.1
db.port=3306
db.name=cadence
db.user=cadence
db.password=change-me
