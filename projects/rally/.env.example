# Rally environment configuration (copy to .env — never commit .env)

# Origin only — no path, no trailing slash
APP_URL=https://cubixmeow.com

# Path to public/ as it appears in the browser URL (no trailing slash).
# Same pattern as SousMeow on this host.
APP_BASE_PATH=/iain/projects/rally/public
