# Local configuration carries credentials; only the example is tracked.
config/config.php
.env

# Runtime state: SQLite database and development clock override.
storage/*.sqlite
storage/*.sqlite-shm
storage/*.sqlite-wal
storage/clock_override.txt
